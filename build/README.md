# Application Icons

Please place your application icons in this directory:

- `icon.ico`: Windows icon (256x256 recommended)
- `icon.png`: macOS/Linux icon (512x512 recommended)

The current `package.json` is configured to look for `build/icon.ico` for Windows.
